# This workflow has been superseded by ci.yml
# Kept for historical reference only.
# See .github/workflows/ci.yml for the active pipeline.
name: CI Pipeline (legacy redirect)

on:
  push:
    branches: [ main ]

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Active CI is in ci.yml — this file is deprecated."
